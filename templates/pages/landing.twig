{% extends "base.twig" %}

{% block content %}
<div class="relative bg-gradient min-h-screen">
    <!-- Decorative Circles -->
    <div class="circle circle-blue"></div>
    <div class="circle circle-orange"></div>

    <!-- Hero Section -->
    <section class="container">
        <div>
            <h1>
                Ticket<span class="text-primary">Flow</span>
            </h1>
            <p class="text-gray text-lg">
                Manage your team's issues seamlessly — create, track, and resolve tickets faster across all projects.
            </p>
            <div style="margin-top: 1.5rem;">
                <a href="/auth/login" class="btn btn-primary">Login</a>
                <a href="/auth/register" class="btn btn-outline">Get Started</a>
            </div>
        </div>

        <!-- Hero Illustration -->
        <div>
            <img src="/assets/images/illustration.svg" alt="Team collaborating" class="hero-image" />
        </div>
    </section>

    <!-- Wave SVG -->
    <svg class="wave" viewBox="0 0 1440 320" xmlns="http://www.w3.org/2000/svg">
        <path fill="#2563eb" fill-opacity="0.3" d="M0,160L40,186.7C80,213,160,267,240,261.3C320,256,400,192,480,149.3C560,107,640,85,720,96C800,107,880,149,960,176C1040,203,1120,213,1200,186.7C1280,160,1360,96,1400,64L1440,32L1440,320L0,320Z"></path>
    </svg>

    <!-- Feature Section -->
    <section class="features">
        {% for feature in features %}
        <div class="feature-card">
            <h3>{{ feature.title }}</h3>
            <p>{{ feature.text }}</p>
        </div>
        {% endfor %}
    </section>
</div>
{% endblock %}
